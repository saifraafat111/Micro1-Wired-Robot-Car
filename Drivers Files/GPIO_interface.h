#ifndef GPIO_CONFIG_H_
#define GPIO_CONFIG_H_

/* =====================================================================
 *  GPIO_config.h — ملف إعدادات (Configuration) لطبقة الـ GPIO
 *
 *  دلوقتي مفيهوش أي إعدادات قابلة للتعديل — متروك فاضي عمدًا
 *  عشان لو احتجنا في المستقبل نضيف #defines أو خيارات إعدادات
 *  بتغيّر سلوك الـ GPIO من غير ما نلمس باقي الكود.
 * ===================================================================== */

#endif /* GPIO_CONFIG_H_ */
